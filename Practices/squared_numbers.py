# DJ, 1st, Squared Numbersn

# loops through numbers 1-20, printing the squared version
print(list(map(lambda n:n**2,range(1,21))))


# loops through numbers 1-20, printing the original number and the squared number
for n in(range(1,21)):print(n,n**2)
